@model Frontend_Sistema_Votaciones.Models.MunicipioViewModel

@{
    ViewData["Title"] = "tbMunicipios".Substring(2);
    ViewData["ViewName"] = "_Edit".Substring(1);
}
<h1>Editar </h1>
<h4>@ViewData["Title"]</h4>
<hr />
<div>
    <div>
        <form asp-action="@ViewData["ViewName"]" asp-controller="Departamento" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="d-flex flex-wrap justify-content-around">
                <input type="hidden" id="Muni_Codigo" name="Muni_Codigo" />
                <div class="form-group col-sm-5 mb-4">
                    <label for="Muni_Descripcion" class="control-label"></label>
                    <input id="Muni_Descripcion" name="Muni_Descripcion" class="form-control" @*value="@Model.Muni_Descripcion"*@ />
                    <span asp-validation-for="Muni_Descripcion" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group d-flex justify-content-end">
                <div class="form-row">
                    <input type="submit" value="Guardar" class="btn btn-primary" />
                    <a asp-action="Index" class="btn btn-outline-primary">Cancelar</a>
                </div>
            </div>
        </form>
    </div>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}